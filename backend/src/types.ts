// backend/src/types.ts
// Currently empty, but can be used for shared types between modules if needed.
// For instance, if NargoManager types become more complex or are used by gameManager directly.
export {};